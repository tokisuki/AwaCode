# AwaCode 编程智能体设计方案

> 状态：实施进行中
> 日期：2026-08-31
> 目标：在 2026 年 9 月 2 日 24:00 前完成一个可真实读写代码、执行命令并自主完成编程任务的桌面 Coding Agent。

## 1. 题目约束与设计原则

本项目严格遵守《推免考核题目学生版》的要求：

- 不使用 LangChain、LlamaIndex、OpenAI Agents SDK、Claude Agent SDK、AutoGen、CrewAI 等 Agent 框架。
- 不封装现有 Agent 产品，也不依赖 API 服务端托管的文件、代码执行或 Code Interpreter 能力。
- 自行实现对话历史与上下文管理、工具定义与本地执行、模型输出解析、Agent 循环、终止条件和错误恢复。
- 仅使用模型厂商的底层 API 客户端、模型原生 tool calling、Node.js 标准库（包括 `node:sqlite`）和 Qt。
- API Key 只从环境变量或 `%LOCALAPPDATA%\AwaCode\auth.json` 读取，不写入仓库、SQLite、README 或演示视频。
- OpenCode 仅作为设计参考，不复制其代码，也不把本地 `opencode/` 目录提交到本仓库。

设计优先级如下：

1. Agent 核心行为正确、可解释、可测试。
2. Qt 桌面端形成完整操作闭环。
3. 错误和权限边界清楚，不把普通命令执行伪装成真正的操作系统沙箱。
4. 控制功能范围，保证两分钟视频可以稳定演示。

## 2. 已确定的技术选型

| 项目 | 选择 |
| --- | --- |
| 桌面端 | C++17、Qt 6 Widgets |
| Agent 核心 | TypeScript、Node.js 24 LTS、npm |
| 模型协议 | OpenAI-compatible Chat Completions |
| 模型能力 | 流式文本、原生 function/tool calling |
| 进程通信 | JSON-RPC 2.0 over NDJSON/stdin/stdout |
| 持久化 | Node.js 内置 `node:sqlite`、SQLite WAL |
| 本地数据 | `%LOCALAPPDATA%\AwaCode\` |
| 构建 | TypeScript Compiler、CMake、Ninja、Qt 6 MinGW kit |
| 测试 | Node.js `node:test`、Qt Test、脚本化假 LLM |

首版明确不做：

- Qt 5 兼容和 Bun 运行时。
- Anthropic 等第二套原生模型协议。
- 多会话并发执行和崩溃后自动重放工具；首版支持多会话历史、持久化和人工继续。
- 自动提炼长期记忆、记忆候选确认界面和复杂记忆管理中心。
- 文件树、代码编辑器、内嵌终端等迷你 IDE 功能。
- MCP、子 Agent、LSP、插件系统和远程执行环境。
- 基于容器或虚拟机的操作系统级隔离。

## 3. 总体架构

项目采用 Qt 展示层、NDJSON 传输层、TypeScript 核心层组成的双进程架构。

```text
┌──────────────────────────────────────────────┐
│ Qt 6 Desktop                                │
│ 工作区选择 / 对话 / 工具时间线 / 审批 / 取消 │
└──────────────────────┬───────────────────────┘
                       │ JSON-RPC 2.0 + NDJSON
                       │ stdin / stdout
┌──────────────────────▼───────────────────────┐
│ TypeScript Core                             │
│ RPC Server → Orchestrator → LLM / Context   │
│             │           └→ Tool Registry     │
│             └→ SessionStore / MemoryStore    │
└──────────────────────────────────────────────┘
```

### 3.1 Qt 端职责

- 用 `QProcess` 启动 `node core/dist/index.js`。
- 选择并传递工作区，不把 Qt 进程当前目录隐式当作工作区。
- 列出、新建和打开当前项目的历史会话。
- 解析 JSON-RPC 响应和通知，展示计划、文本增量、工具状态和错误。
- 对编辑、创建文件和命令执行进行“允许一次/拒绝”审批。
- 提供模型配置页；未配置模型时允许浏览历史但禁止运行 Agent。
- 提供运行、取消、查看 stderr 和手动重启核心进程的能力。

Qt 端不得拼接系统 Prompt、调用模型或执行 Agent 工具。

### 3.2 TypeScript 端职责

- 管理 SQLite 迁移、会话历史、上下文预算、滚动摘要和中断恢复。
- 解析稳定项目身份，管理全局/项目记忆文件及其系统上下文快照。
- 调用 OpenAI-compatible API，解析流式文本和 tool calls。
- 管理 Plan、Execute、Reflect 三阶段状态。
- 注册、校验、审批和执行本地工具。
- 控制轮次、工具次数、重复调用、取消、重试和最终结束。

TypeScript 端不包含任何 Qt 或界面逻辑。

支持的 Headless CLI 为 `node core/dist/index.js`；它是 Qt 之外受支持的无界面核心入口，使用与桌面端相同的 stdin/stdout JSON-RPC 协议。

### 3.3 本地数据边界

运行数据不写入用户选择的代码仓库：

| 路径 | 内容 |
| --- | --- |
| `%LOCALAPPDATA%\AwaCode\awacode.db` | 项目、会话、消息、工具事件和上下文快照 |
| `%LOCALAPPDATA%\AwaCode\config.json` | Base URL、模型与上下文参数 |
| `%LOCALAPPDATA%\AwaCode\auth.json` | 用户选择保存的 API Key |
| `%LOCALAPPDATA%\AwaCode\memory\global.md` | 全局长期记忆 |
| `%LOCALAPPDATA%\AwaCode\memory\projects\<project-id>.md` | 项目长期记忆 |

仓库内 `/.local/opencode-learning.md` 是开发者个人学习记录，整个 `/.local/` 已加入 `.gitignore`。正式设计决策只记录在本文档。

## 4. JSON-RPC 与进程生命周期

### 4.1 分帧规则

- 每一行是一个完整 JSON-RPC 2.0 对象，即 NDJSON。
- 消息中的换行必须经过 JSON 转义，不能直接跨协议行输出。
- stdout 只能发送协议消息；日志全部写入 stderr。
- 两端都必须支持一次读取到半条、多条或混合消息的情况。
- 单条协议行最多 1 MiB（1,048,576 字节），不含换行分隔符和可选的 CR；超过上限立即作为协议错误拒绝。
- 分帧以 LF 为界，接受 CRLF；空行和只含空白的行忽略。
- NDJSON 解码器一旦遇到解析错误、超长行或未完成的 EOF 行即进入终止状态，之后所有输入都返回同一个错误，不能继续在不明确的状态上解析。
- EOF 时只允许遗留空白；任何非空白的未换行尾部都是 `incomplete_line` 协议错误。
- Qt 发出的请求 ID 使用 `ui-*`，核心发出的反向请求 ID 使用 `core-*`。
- 每个 peer 的出站请求计数从 1 开始并独立递增，因此 ID 精确形如 `ui-1`、`ui-2` 或 `core-1`、`core-2`；pending 映射必须在写出请求前建立，以支持同步回送和乱序响应。
- 每条连接只有一个逻辑接收者负责 NDJSON 解码并把完整值交给同一个双向 `JsonRpcPeer`；单个请求不得从共享响应队列自行读取。接收者可以在已有 handler 等待反向请求结果时继续分派后续响应。
- `stdin` 正常 EOF 时先调用解码器 `end()`，再关闭 peer、拒绝全部 pending 请求并排空已排队的 stdout 写入。关闭是幂等的；关闭后的请求、通知和接收都以类型化的 `RpcDisconnectedError` 拒绝，已取消请求的迟到响应直接忽略。
- 遇到非法 JSON 时只发送一次 ID 为 `null` 的 `-32700` 响应，随后关闭 peer；超长行或 EOF 未完成行只写 stderr 诊断，不伪造请求或响应。stdout 始终只包含协议记录。

JSON-RPC 错误码固定如下，handler 主动抛出的 `RpcFault` 保留声明的 code、message 和 data；其他参数解析异常映射为 `-32602`，其他 handler 异常必须脱敏为 `-32603`，不得泄漏栈或本地路径。

| 类别 | 名称 | 错误码 |
| --- | --- | ---: |
| 标准 | parse error | -32700 |
| 标准 | invalid request | -32600 |
| 标准 | method not found | -32601 |
| 标准 | invalid params | -32602 |
| 标准 | internal error | -32603 |
| 应用 | busy | -32001 |
| 应用 | not configured | -32002 |
| 应用 | not found | -32003 |
| 应用 | history integrity | -32004 |
| 应用 | cancelled | -32005 |

### 4.2 Qt → Core 请求

| 方法 | 参数 | 结果 |
| --- | --- | --- |
| `core/hello` | `{}` | 核心版本、数据库版本、配置和中断恢复数量 |
| `workspace/set` | `{ workspace }` | 规范化工作区和稳定项目 ID |
| `session/list` | `{ projectId }` | 当前项目的会话摘要列表 |
| `session/create` | `{ projectId, title? }` | 新会话 ID 与初始元数据 |
| `session/load` | `{ sessionId }` | 可展示的完整历史和会话状态 |
| `agent/run` | `{ sessionId, prompt }` | 最终文本、状态、轮次和工具次数 |
| `agent/cancel` | `{}` | 是否成功发出取消 |
| `config/status` | `{}` | 脱敏后的模型配置状态 |
| `config/save` | `{ baseUrl, model, limits, apiKey? }` | 脱敏后的保存结果 |
| `config/test` | `{}` | 用户主动触发的连接测试结果 |
| `memory/read` | `{ projectId }` | 当前全局和项目记忆文本 |

`agent/run` 在任务真正结束时才返回；执行中的增量信息通过通知发送。同一时间只允许一个任务，第二个 `agent/run` 返回 busy 错误。

当前持久化基础阶段的五个方法使用以下精确结果形状：

- `core/hello {}` → `{coreVersion:"0.1.0",databaseVersion:1,configured:false,interruptedCount:0}`。
- `workspace/set {workspace}` → `{workspace:<规范化真实绝对路径>,projectId:<稳定项目 ID>}`。
- `session/list {projectId}` → `SessionSummary[]`，其中 `SessionSummary` 为 `{id,projectId,title,model,status,createdAt,updatedAt}`，按 `updatedAt` 从新到旧排列；未绑定模型时 `model` 为 `null`。
- `session/create {projectId,title?}` → 一个 `SessionSummary`；省略 `title` 时标题精确为 `New session`，初始状态为 `idle`。
- `session/load {sessionId}` → `{session:SessionSummary,messages:MessageRecord[],toolCalls:ToolCallRecord[]}`；消息按 `seq` 升序，工具调用按所属 assistant 消息的 `seq`、再按 `ordinal` 升序。

这五个方法的参数都必须是只包含声明字段的对象；缺字段、额外字段或字段类型错误返回 `-32602`。不存在的工作区、项目或会话返回 `-32003`，消息分别为 `Workspace not found`、`Project not found`、`Session not found`，`data` 分别携带 `{workspace}`、`{projectId}`、`{sessionId}`。

### 4.3 Core → Qt 通知和请求

| 类型 | 方法 | 用途 |
| --- | --- | --- |
| 通知 | `agent/phase` | 切换 plan、execute、reflect 等阶段 |
| 通知 | `stream/text` | 输出文本 delta；字段为 `{runId,eventSeq,messageId,phase,delta,provisional}` |
| 通知 | `stream/commit` | 将对应的 provisional 流式消息最终定稿 |
| 通知 | `tool/start` | 展示工具名称和参数摘要 |
| 通知 | `tool/end` | 展示状态、耗时、结果和截断信息 |
| 通知 | `agent/status` | busy、done、cancelled、error |
| 通知 | `memory/updated` | 展示记忆作用域和脱敏摘要 |
| 请求 | `permission/request` | 请求写入或命令执行审批 |

`stream/text` 的 `runId` 标识本次 Agent 运行，`eventSeq` 在该运行内严格递增，`messageId` 标识正在生成的 assistant 消息，`phase` 为当前阶段，`delta` 为新增文本，`provisional` 表示该文本尚未最终定稿。`stream/commit` 使用相同的 `runId`、`eventSeq` 和 `messageId` 标识最终化，届时 Qt 将该消息作为最终内容呈现。

`permission/request` 的参数包含工具调用 ID、风险种类、标题和预览；Qt 返回 `allow_once` 或 `deny`。

### 4.4 生命周期

- Qt 检测核心异常退出，保留已有界面内容并显示 stderr。
- 不使用原方案中“500ms ping、1.5s kill”的激进看门狗。
- 用户可以手动重启核心；旧会话可以恢复显示和继续对话。
- 启动时先执行工具调用收敛事务：将遗留的 `pending`、`awaiting_approval`、`running` 工具标记为 `interrupted`，为每个未闭合调用写入配对的合成错误结果，再允许会话继续；不得自动重放副作用。
- 取消会触发核心 `AbortController`，同时终止正在运行的命令进程树，并将尚未批准的调用标记为 `interrupted`；取消不是用户拒绝审批，不能记为 `denied`。

## 5. 核心数据契约

```ts
type AgentPhase =
  | "idle"
  | "plan"
  | "execute"
  | "reflect"
  | "done"
  | "cancelled"
  | "error"

type SessionStatus =
  | "idle"
  | "running"
  | "completed"
  | "interrupted"
  | "cancelled"
  | "error"

type ApprovalKind = "none" | "write" | "command"

type ToolCallStatus =
  | "pending"
  | "awaiting_approval"
  | "running"
  | "success"
  | "failure"
  | "denied"
  | "interrupted"

interface ToolDefinition<T> {
  name: string
  description: string
  inputSchema: Record<string, unknown>
  approval: ApprovalKind
  validate(value: unknown): T
  execute(input: T, context: ToolContext): Promise<ToolResult>
}

interface ToolResult {
  status: "success" | "failure" | "denied" | "interrupted"
  summary: string
  content: string
  durationMs: number
  metadata: Record<string, unknown>
}
```

工具定义中的 JSON Schema 提供给模型；`validate` 仍必须对模型返回的未知输入做本地验证。无效 JSON、未知工具和参数错误都会转换成模型可理解的 tool result，而不是直接让 Agent 崩溃。

### 5.1 SQLite 数据模型

SQLite 只保存运行与审计状态，不设置自动提炼记忆的 `memory` 表。

数据根目录优先使用非空的 `AWACODE_DATA_DIR`，否则 Windows 使用 `%LOCALAPPDATA%\AwaCode\`；两者都不可用时必须拒绝启动。路径先规范化为绝对路径，再创建数据根和 `backups/`。该根下固定暴露 `awacode.db`、`config.json`、`auth.json`、`memory/` 和 `backups/`；测试与便携运行必须给每次运行提供唯一的 `AWACODE_DATA_DIR`，不得读写真实用户目录。

每个 SQLite 连接在提供服务前启用 `foreign_keys=ON`、`journal_mode=WAL`、`synchronous=NORMAL` 和 `busy_timeout=5000`。初始化器先取得跨进程的 `BEGIN IMMEDIATE` 写锁，再在锁内重新识别数据库、读取全部已应用版本、决定是否备份并执行全部缺失迁移；schema/data 变化与对应 `schema_migrations` 行在同一事务中提交，任一步失败就完整回滚。新空库不备份；可识别且非空的旧库在升级前通过独立只读连接执行 SQLite 在线备份，写入 `backups/awacode-v<旧版本>-<Windows 安全 UTC 时间戳>.db`。若该名称已经存在，原文件绝不覆盖，依次使用 `-1`、`-2` 等数字后缀原子保留新名称。失败升级保留可用源库和已完成备份。只要 `sqlite_schema` 中存在名称不以 `sqlite_` 开头的表、视图、索引或触发器，数据库就视为非空；非空库若没有结构可识别的 `schema_migrations(version,applied_at)` 表，必须拒绝而不能猜测、覆盖或创建替代空库。重复初始化和并发初始化都只能留下每个版本一行。

V1 的所有表均为 SQLite `STRICT` 表，精确列与约束如下：

```text
schema_migrations(
  version INTEGER PRIMARY KEY,
  applied_at TEXT NOT NULL
)

projects(
  id TEXT PRIMARY KEY,
  identity_kind TEXT NOT NULL CHECK (identity_kind IN ('remote','root','path')),
  identity_value TEXT NOT NULL,
  remote TEXT,
  root_path TEXT NOT NULL,
  created_at TEXT NOT NULL,
  updated_at TEXT NOT NULL
)

sessions(
  id TEXT PRIMARY KEY,
  project_id TEXT NOT NULL REFERENCES projects(id),
  title TEXT NOT NULL,
  model_json TEXT,
  status TEXT NOT NULL CHECK (status IN ('idle','running','completed','interrupted','cancelled','error')),
  created_at TEXT NOT NULL,
  updated_at TEXT NOT NULL
)
INDEX sessions_project_updated_idx(project_id, updated_at DESC)

messages(
  id TEXT PRIMARY KEY,
  session_id TEXT NOT NULL REFERENCES sessions(id) ON DELETE CASCADE,
  seq INTEGER NOT NULL,
  role TEXT NOT NULL CHECK (role IN ('system','user','assistant','tool','internal')),
  kind TEXT NOT NULL,
  payload_json TEXT NOT NULL,
  status TEXT NOT NULL CHECK (status IN ('streaming','complete','interrupted')),
  created_at TEXT NOT NULL,
  updated_at TEXT NOT NULL,
  UNIQUE(session_id, seq)
)

tool_calls(
  call_id TEXT PRIMARY KEY,
  session_id TEXT NOT NULL REFERENCES sessions(id) ON DELETE CASCADE,
  assistant_message_id TEXT NOT NULL REFERENCES messages(id) ON DELETE CASCADE,
  ordinal INTEGER NOT NULL,
  tool_name TEXT NOT NULL,
  input_text TEXT NOT NULL,
  status TEXT NOT NULL CHECK (status IN ('pending','awaiting_approval','running','success','failure','denied','interrupted')),
  result_json TEXT,
  error_text TEXT,
  created_at TEXT NOT NULL,
  started_at TEXT,
  finished_at TEXT,
  UNIQUE(assistant_message_id, ordinal)
)
INDEX tool_calls_session_idx(session_id)

context_snapshots(
  session_id TEXT PRIMARY KEY REFERENCES sessions(id) ON DELETE CASCADE,
  baseline TEXT NOT NULL DEFAULT '',
  source_snapshot_json TEXT NOT NULL DEFAULT '{}',
  baseline_seq INTEGER NOT NULL DEFAULT 0,
  summary TEXT,
  summary_upto_seq INTEGER NOT NULL DEFAULT 0,
  updated_at TEXT NOT NULL
)
```

项目 ID 按下节的稳定身份计算；会话、消息等本地实体 ID 使用 `crypto.randomUUID()`，provider 给出的工具 `call_id` 原样持久化。所有持久化时间戳都使用 UTC `Date#toISOString()`；测试通过注入 ID 和时钟获得确定结果。

| 表 | 关键字段 | 职责 |
| --- | --- | --- |
| `projects` | `id`、`remote`、`root_path`、时间戳 | 稳定识别项目并维护最近路径 |
| `sessions` | `id`、`project_id`、`title`、`model_json`、`status`、时间戳 | 会话元数据与恢复状态 |
| `messages` | `id`、`session_id`、`seq`、`role`、`kind`、`payload_json`、`status`、时间戳 | 按严格顺序保存用户、assistant 消息及流式完成状态 |
| `tool_calls` | `call_id`、`session_id`、`assistant_message_id`、`ordinal`、`tool_name`、`input_text`、`status`、`result_json`、`error_text`、开始/结束时间 | 保存原始参数、调用状态和唯一配对结果，支持参数错误回馈、崩溃收敛与协议完整性检查 |
| `context_snapshots` | `session_id`、`baseline`、`source_snapshot_json`、`baseline_seq`、`summary`、`summary_upto_seq` | 持久化系统上下文基线和滚动摘要 |

`messages` 对 `(session_id, seq)` 建唯一索引，不能依赖时间戳猜测恢复顺序。`tool_calls.call_id` 唯一，且引用产生它的 assistant 消息；一个调用只能有一个终态结果。SQLite 启用外键、WAL 和 `busy_timeout`；数据库版本迁移全部在事务中完成，迁移前创建本地备份。

迁移成功后 `core/hello` 返回数据库版本和恢复到的中断数量。迁移失败时 Core 通过 stderr 报告脱敏错误和数据库路径后退出，不覆盖原库，也不悄悄创建空库继续运行。

### 5.2 先落盘、后执行

持久化顺序是安全边界的一部分：

1. 用户输入写入 `messages` 后，才能启动模型调用。
2. assistant 流完整结束、provider 已给出完整工具调用边界和稳定 `call_id` 后，在同一事务中写入 assistant 消息、全部 `pending` 工具调用及原始参数文本；提交之前不得校验、申请审批或执行工具。
3. 提交后再解析原始参数并执行本地 Schema 验证；无效 JSON、未知工具或参数错误执行 `pending → failure` 并写入配对错误结果。不需审批的有效工具以条件更新将 `pending → running`；需要审批的有效工具先执行 `pending → awaiting_approval`。用户允许时执行 `awaiting_approval → running`，用户拒绝时执行 `awaiting_approval → denied`。每次状态更新必须先提交，再开始下一步或产生副作用。
4. 工具完成后将 `running` 转为 `success` 或 `failure` 并写入结果；崩溃或取消将任一非终态转为 `interrupted`。状态更新采用事务或 compare-and-swap 条件，执行器发现调用已是终态时必须拒绝再次执行。
5. 正常结束后将会话设为 `completed`；Core 异常退出遗留的运行状态在下次启动时收敛为 `interrupted`。
6. 恢复会话只重放界面与已经闭合的模型历史，不自动重新执行工具；用户提交新消息后才产生新的 Agent 运行。

合法状态机如下：

```text
pending
├─ 无需审批 ───────────────→ running
├─ 需要审批 ───────────────→ awaiting_approval
├─ 工具或参数无效 ─────────→ failure
└─ 崩溃或取消 ─────────────→ interrupted

awaiting_approval
├─ allow ──────────────────→ running
├─ deny ───────────────────→ denied
└─ 崩溃或取消 ─────────────→ interrupted

running
├─ 正常完成 ───────────────→ success
├─ 执行错误 ───────────────→ failure
└─ 崩溃或取消 ─────────────→ interrupted
```

`success`、`failure`、`denied`、`interrupted` 都是终态，不能再转移。`failure` 既可表示执行前的工具/参数验证错误，也可表示开始执行后的操作系统权限错误、命令非零退出或运行时错误。`denied` 只允许由 `awaiting_approval` 进入，含义是工具从未启动、没有产生副作用；`running → denied` 非法。审批允许与取消/拒绝发生竞态时，只有一个条件更新能成功；失败的一方读取终态后停止。

### 5.3 稳定项目 ID

参考 OpenCode 的项目解析，但不在仓库中写项目缓存文件：

1. Git 项目优先读取非空的 `remote.origin.url`。非本地的 scp 和 URL 形式都规范化为 `lowercase-host/lowercase-path`：移除 scheme、凭据、query、fragment、默认端口、末尾 `/` 和 `.git`，然后使用身份输入 `remote:<规范化 remote>`。Windows 盘符路径（无论 `/` 或 `\`）、UNC 路径和 `file:` URL 属于本地 origin，不能提供跨克隆稳定身份；这些形式不进入 scp 解析，而是继续回退到根提交或路径身份。
2. 没有 remote 时执行 `git rev-list --max-parents=0 HEAD`，多根历史选择按字典序最小的根提交哈希，身份输入为 `root:<hash>`。
3. Git 仓库尚无提交，或目录不是 Git 仓库时，使用 `path:<规范化真实绝对路径>`；Windows 仅在身份输入中把路径转为小写，返回和保存的当前路径仍保留真实规范化形式。
4. 项目 ID 是上述身份输入的 SHA-256 小写十六进制。数据库分别保存 `identity_kind` 与不含前缀的 `identity_value`，当前 `root_path` 只是可更新别名；重访同一身份只更新路径和 `updated_at`。

这样同一远程仓库的不同 clone 可以共享项目记忆，同时避免不同仓库串用上下文。

## 6. 单 Orchestrator 三阶段设计

不实现三个互相独立的 Agent，也不提供三种用户可切换模式。一个 Orchestrator 依次执行 Plan、Execute 和 Reflect。

### 6.1 Plan

- 接收用户任务和系统约束。
- 禁用所有工具，单独调用模型一次。
- 生成最多约六条短计划并通过 `agent/phase` 和文本事件展示。
- 将计划作为内部消息加入历史，然后进入 Execute。

### 6.2 Execute

- 调用模型并允许 `tool_choice: auto`。
- 流式文本直接发送给 Qt。
- 按 tool-call index 累积名称与参数字符串，完整结束后再解析和校验。
- 一次返回多个工具调用时，按模型给出的顺序串行审批和执行。
- 将 assistant tool calls 和所有对应 tool results 加入历史，再进行下一轮模型调用。
- 模型首次在没有工具调用时自然结束，产生候选最终回复并进入 Reflect。

### 6.3 Reflect

- 禁用工具，要求模型根据计划、执行记录和测试证据返回结构化的 `complete/continue + reason`。
- 如果为 `complete`，接受候选最终回复。
- 如果为 `continue`，将反馈加入历史并允许一次补救执行。
- 补救结束后不再递归反思，避免形成另一层无限循环。

### 6.4 终止条件

以下任一条件都必须使任务在有限时间内结束：

- Reflection 判断完成。
- Execute 最多进行 12 次模型轮次。
- 整个任务最多执行 24 个工具调用。
- 连续三次调用同一工具且规范化参数相同，判定为重复循环。
- 用户取消、核心进程退出或出现不可恢复的模型错误。

达到轮次或工具上限时，核心禁用工具并要求模型生成收尾说明，明确列出已完成、未完成和停止原因，不能静默结束。

## 7. 上下文管理

完整消息和工具事件都持久化在 SQLite。发送模型前，自研 `ContextManager` 从系统上下文基线、记忆、摘要和压缩点后的消息构造实际请求；数据库历史与模型当前可见上下文必须明确区分。

### 7.1 构造顺序

每次模型调用的上下文按以下顺序构造：

1. AwaCode 系统提示、工具契约和当前工作区环境。
2. 全局长期记忆。
3. 当前项目长期记忆；发生冲突时项目内容优先。
4. 最近一次持久化滚动摘要。
5. 摘要覆盖序号之后的完整用户、助手和工具交互。

### 7.2 系统上下文基线

参考 OpenCode 的 `system context` 与 `context epoch`：

- 会话首次运行时保存模型实际接收的系统上下文 `baseline`、各来源内容哈希和 `baseline_seq`。
- 每次模型调用前重新读取运行环境、全局记忆和项目记忆。
- 来源未变化时不重复注入；变化时追加系统上下文更新事件并持久化新快照。
- 来源暂时无法读取时保留最后一次成功快照，不能把读取故障误判为内容已删除。
- 完成历史压缩后，以当前来源建立新的 baseline，避免更新消息无限累积。
- Core 重启后从持久化 baseline、snapshot 和消息序号恢复等价上下文。

### 7.3 预算与估算

不使用固定“75%”触发比例，也不固定保留“最近六块”。参数参考 OpenCode 的自适应压缩策略：

```text
contextLimit   = AWACODE_CONTEXT_LIMIT，默认 32768
maxOutput      = AWACODE_MAX_OUTPUT_TOKENS，默认 4096
usable         = contextLimit - maxOutput
recentBudget   = clamp(floor(usable * 0.25), 2000, 15000)
summaryOutput  = min(maxOutput, 4096)
```

默认配置下：

```text
usable       = 32768 - 4096 = 28672
recentBudget = floor(28672 * 0.25) = 7168 tokens
```

- Base URL 兼容网关通常不能提供模型目录和上下文上限，因此允许用户在设置页覆盖两个限制。
- ASCII 文本约按四字符一个 token，非 ASCII 文本按一个字符一个 token，并计入系统消息与工具 Schema 开销。
- OpenCode 使用统一“字符数除以四”；这里对中文做保守修正，避免严重低估。
- API 返回 usage 时记录真实 token 数据用于诊断，但不依赖特定厂商 tokenizer。

### 7.4 原子交互块

assistant 的 tool calls 与其后所有对应 tool results 组成不可拆分的原子块。这里的“原子”是模型协议与上下文选择不变量，不是把 SQLite、文件系统和子进程副作用包装成一个分布式事务。

恢复时必须先在单个 SQLite 事务中执行收敛：

1. 将遗留的 `running` 会话标记为 `interrupted`。
2. 找出所有 `pending`、`awaiting_approval`、`running` 工具调用，仅当其仍是非终态时更新为 `interrupted`，并写入与原 `call_id` 配对的合成错误结果。
3. `pending` 或 `awaiting_approval` 的合成结果明确说明“执行从未开始，没有本地副作用”；`running` 的合成结果明确说明“持久化结果缺失，副作用可能已经发生，重试前必须检查工作区”。
4. 保留同一 assistant 消息中已经落盘的真实终态结果，只修复未闭合调用；条件更新与结果唯一约束使该过程可重复执行而不产生重复结果。

每次调用模型前再次运行轻量收敛与历史完整性检查。模型可见历史必须满足：每个完整 assistant tool call 恰好有一个相同 `tool_call_id` 的终态 tool result；不存在孤立结果、重复结果或非终态调用。若修复后仍不满足，则返回 `history_integrity_error`，不得把畸形历史发给 API。适配器将 `success` 序列化为正常 tool result，将 `failure`、`denied`、`interrupted` 序列化为配对的错误 tool result。

如果 Core 在 assistant 流尚未形成一组完整、可解析的 tool calls 时崩溃，则把该 assistant 消息标记为 `interrupted`，在界面和审计历史中保留，但从模型历史中排除。由于工具只能在完整调用集合落盘后开始，此分支不会产生工具副作用。若在工具已经产生副作用、但结果提交前崩溃，系统只能诚实记录“结果未知”；恢复时仍不自动重放。

压缩、选择近期内容或构造模型请求时不能产生缺少 tool call 的 tool result，也不能保留没有结果的 tool call。近期内容以完整原子块从新到旧填满 `recentBudget`。

### 7.5 滚动摘要

- 对近期预算以前的内容生成结构化摘要，包含目标、约束与决策、已完成工作、当前状态、阻塞、下一步和相关文件。
- 下一次压缩使用旧摘要和新淘汰内容生成替代摘要，并持久化 `summary_upto_seq`。
- 摘要最大输出为 4096 tokens；送入摘要模型的单个工具输出最多保留 2000 字符。
- 完整消息和完整工具输出不从 SQLite 删除；截断仅影响摘要请求和主模型请求。
- 摘要失败时不无限重试，也不声称压缩成功；如果请求仍超预算，则返回明确错误并建议提高上下文限制或开启新会话。
- API 返回 context overflow 时强制尝试压缩；压缩成功后只重试原请求一次，第二次溢出直接报告。

### 7.6 跨会话长期记忆

OpenCode 没有通用的自动记忆提炼表，而是依靠全局/项目指令文件，并在特定模型提示中允许用户明确要求时更新记忆文件。AwaCode 采用相同的显式原则：

- 不自动从普通聊天提炼记忆，不产生候选，也不设置候选确认界面。
- 用户明确说“记住……”时才允许 Agent 调用 `memory_write`；未说明作用域时默认项目级，明确说“全局、所有项目、我的偏好”时使用全局级。
- 用户明确说“忘掉……”时使用同一工具精确删除或改写相关记忆。
- 记忆写入不二次弹窗，因为当前用户指令本身就是授权；但调用内容、作用域、时间和结果必须进入会话审计事件。
- 记忆文件采用临时文件加原子替换，写入失败作为工具错误返回，不能假装已经记住。

## 8. 工具集合

首版只实现六个工作区工具和一个严格限定目录的记忆工具。

### 8.1 `list_files`

参数：`path?`、`max_depth?`。

- 递归列出工作区内相对路径。
- 默认忽略 `.git`、`node_modules`、`dist` 和 `build`。
- 限制最大深度和返回数量，并报告是否截断。

### 8.2 `read_file`

参数：`path`、`offset_line?`、`limit_lines?`。

- 输出带行号文本。
- 支持分页读取。
- 单次最多返回 50 KiB。
- 拒绝目录、二进制文件和工作区外路径。

### 8.3 `search_text`

参数：`query`、`path?`、`is_regex?`。

- 在工作区文本文件中递归搜索。
- 限制单文件大小、匹配数量和输出大小。
- 跳过常见依赖、构建目录和二进制文件。

### 8.4 `edit_file`

参数：`path`、`old_text`、`new_text`、`replace_all?`。

- 只做精确字符串替换。
- 默认要求 `old_text` 只匹配一次；零次或多次时返回可修正错误。
- 审批前展示路径和替换前后预览。
- 审批后再次确认文件内容未变化，再原子写入临时文件并替换目标文件。

### 8.5 `write_file`

参数：`path`、`content`。

- 只允许创建新文件。
- 文件已存在时拒绝覆盖，提示模型使用 `edit_file`。
- 审批前展示路径、大小和内容预览。

### 8.6 `run_command`

参数：`command`、`cwd?`、`timeout_ms?`。

- cwd 必须位于工作区，默认使用工作区根目录。
- 默认超时 60 秒，最大 180 秒。
- 分别捕获 stdout、stderr、exit code、超时和取消状态。
- 非零退出码表示命令执行完成但结果失败，仍完整返回给模型分析。
- Windows 使用 PowerShell，其他平台可使用 `/bin/sh -lc`。
- 命令子进程移除 `AWACODE_API_KEY`、`OPENAI_API_KEY` 等模型凭据。
- 取消或超时必须终止整个子进程树。

### 8.7 `memory_write`

参数：`scope`（`project/global`）、`old_text?`、`new_text`。

- 仅能访问 `%LOCALAPPDATA%\AwaCode\memory\` 中由 Core 根据作用域解析出的目标文件，模型不能传入任意路径。
- `old_text` 省略时追加新记忆；提供时必须精确匹配一次，再替换为 `new_text`；将 `new_text` 设为空可用于“忘掉”。
- 系统提示明确限制：只有当前用户明确要求记住、更新或忘掉内容时才能调用。该行为参考 OpenCode 的显式记忆原则，而不是自动提炼。
- “是否明确要求”属于 Agent 行为策略，首版不使用容易误判的关键词正则冒充安全判定；Core 强制执行的硬边界是固定记忆目录、固定作用域和完整审计。
- 用户指令视为本次记忆写入授权，不再触发 `permission/request`。
- 写入采用原子替换，并将调用、作用域、内容摘要和结果持久化到会话事件。

## 9. 工作区策略与权限

### 9.1 文件工具边界

- 相对路径基于用户选择的工作区解析。
- 对既有文件使用 `realpath`；对新文件检查最近的既存父目录。
- 比较规范化后的真实路径，拒绝 `..` 和符号链接逃逸。
- Windows 路径比较忽略盘符和大小写差异。

### 9.2 审批策略

- 读取、列目录和搜索自动允许。
- 创建、编辑和命令执行必须由 Qt 用户允许一次。
- 明确“记住/忘掉”指令触发的 `memory_write` 不二次确认，但只能写 Core 解析出的本地记忆文件。
- 拒绝审批会生成 `denied` 工具结果，模型可以调整方案，但不能绕过审批。
- 暂不实现“本会话始终允许”，避免规则管理扩大范围。

### 9.3 关于“沙箱”的准确表述

文件工具能够可靠地限制在工作区内，但任意 shell 命令仍可能通过绝对路径或自身逻辑访问工作区外资源。首版不具备容器、虚拟机或受限令牌提供的 OS 级隔离，因此模块命名采用 `CommandExecutor` 和 `WorkspaceGuard`，而不是误导性的 `SandboxExecutor`。

命令执行依靠以下措施降低风险：

- 每次显式审批。
- 展示完整命令与 cwd。
- 明确提示命令以当前用户权限运行并可能访问工作区外。
- 超时、取消、输出限制和凭据剥离。

## 10. LLM 网关与错误恢复

### 10.1 配置

用户必须主动配置模型。程序不内置可用账号、默认 API Key 或默认模型；未配置时可以打开工作区和浏览历史，但不能运行 Agent。

Qt 设置页提供：

- OpenAI-compatible Base URL。
- Model ID。
- API Key，或选择从 `AWACODE_API_KEY` 环境变量读取。
- Context Limit，默认建议值 32768。
- Max Output Tokens，默认 4096。

本地文件和环境变量分离：

| 项目 | 本地位置 | 环境变量覆盖 |
| --- | --- | --- |
| Base URL | `%LOCALAPPDATA%\AwaCode\config.json` | `AWACODE_BASE_URL` |
| Model ID | `%LOCALAPPDATA%\AwaCode\config.json` | `AWACODE_MODEL` |
| Context Limit | `%LOCALAPPDATA%\AwaCode\config.json` | `AWACODE_CONTEXT_LIMIT` |
| Max Output Tokens | `%LOCALAPPDATA%\AwaCode\config.json` | `AWACODE_MAX_OUTPUT_TOKENS` |
| API Key | `%LOCALAPPDATA%\AwaCode\auth.json`（仅用户选择保存时） | `AWACODE_API_KEY` |
| Node 路径 | Qt 启动配置 | `AWACODE_NODE_PATH`，默认 `node` |
| Core 入口 | Qt 启动配置 | `AWACODE_CORE_ENTRY` |

优先级固定为“环境变量 > 本地配置文件 > 未配置”。该分离参考 OpenCode 的 provider/model 配置与用户数据目录 `auth.json`，但首版只支持 OpenAI-compatible Chat Completions，不实现完整 provider 目录。

安全要求：

- `config/save` 由 Core 写入本地文件，返回值只能包含 `hasApiKey`，不能回传原始 Key。
- API Key 不进入 SQLite、命令行参数、普通日志、错误详情、README 或视频。
- Qt Key 输入框默认遮挡；Core 必须剥离 Authorization header 和常见凭据字段后再记录诊断信息。
- “测试连接”必须由用户主动触发，失败结果经过脱敏。
- 切换模型后，新任务使用新配置；历史会话保留创建或运行时的模型标识。

选用 Chat Completions 而非 Responses API，是为了兼容更多 OpenAI-compatible 网关。只使用官方 `openai` 客户端，不使用其 Agents SDK。

### 10.2 重试规则

- 429、5xx、连接中断和网络超时最多重试三次。
- 优先尊重 `Retry-After`，否则采用带少量随机抖动的指数退避。
- 400、401、403、无效模型和无效配置不重试，直接报告可操作错误。
- context overflow 交给 ContextManager 压缩后仅重试一次。
- 工具参数错误、文件不存在、权限拒绝和命令失败作为 tool result 返回，不触发模型 API 重试。

## 11. Qt 6 桌面端

界面定位为 Agent 控制台，不是 IDE。

### 11.1 主窗口

- 顶部：工作区路径、选择目录、核心进程状态、当前模型和设置入口。
- 左侧窄栏：当前项目最近会话、“新建会话”和中断状态标记。
- 中部左侧：用户消息、计划、Agent 流式文本和最终结论。
- 中部右侧：工具时间线，显示工具名、输入摘要、审批状态、耗时、退出码和截断状态。
- 底部：多行任务输入、运行按钮、取消按钮。
- 可展开区域：核心 stderr 与协议错误日志。

### 11.2 运行状态

- 空闲时允许更换工作区和提交任务。
- 未配置模型时显示明确提示并禁用运行，但会话浏览、记忆查看和设置仍可用。
- 打开旧会话时从 SQLite 重放可展示历史；`interrupted` 会话只有提交新消息后才继续。
- 运行时禁用工作区切换和再次运行，只保留取消。
- 权限请求用原生对话框展示预览和风险。
- `memory_write` 在工具时间线显示作用域和内容摘要，不弹二次确认。
- 核心异常退出后保留已显示内容，标记任务中断并提供手动重启。

### 11.3 渲染批处理

`readyReadStandardOutput` 只负责收集和解析协议数据。文本 delta 进入缓冲区，由 16ms `QTimer` 批量刷新界面，避免每个 token 都触发控件重绘。该机制称为 UI 渲染批处理，不宣称为完整的协议背压。

## 12. 项目结构（推荐）

```text
AwaCode/
├─ core/
│  ├─ src/
│  │  ├─ protocol/       # NDJSON 与 JSON-RPC
│  │  ├─ agent/          # Orchestrator 与阶段状态
│  │  ├─ context/        # 预算、上下文基线与滚动摘要
│  │  ├─ persistence/    # SQLite、迁移、会话与消息仓储
│  │  ├─ memory/         # 全局/项目记忆与原子写入
│  │  ├─ config/         # 模型配置、凭据读取与脱敏
│  │  ├─ llm/            # OpenAI-compatible 网关
│  │  ├─ tools/          # 工具契约、注册表与本地工具
│  │  └─ security/       # WorkspaceGuard 与审批接口
│  ├─ test/
│  ├─ package.json
│  └─ tsconfig.json
├─ desktop/
│  ├─ src/
│  │  ├─ AgentProcessManager.*
│  │  ├─ RpcCodec.*
│  │  ├─ SessionList.*
│  │  ├─ ModelSettingsDialog.*
│  │  └─ MainWindow.*
│  ├─ test/
│  └─ CMakeLists.txt
├─ demo/
│  ├─ fixture/
│  └─ reset.mjs
├─ .local/               # 已忽略的个人 OpenCode 学习记录
├─ README.md
├─ 设计参考.md
└─ README.txt
```

## 13. 测试与验收

### 13.1 TypeScript 单元测试

- NDJSON 拆包、粘包、Unicode 和非法 JSON。
- 请求 ID 对应、未知方法、busy 和取消。
- `..`、绝对路径和符号链接逃逸。
- 文件读取分页、二进制拒绝和输出截断。
- 精确替换的零匹配、多匹配和审批后文件变化。
- 命令非零退出、超时、取消、进程树终止和凭据剥离。
- SQLite 迁移、会话内 `seq` 唯一性、事务失败和启动中断修复。
- 工具状态机全部合法转移；特别覆盖 `pending → failure`、`awaiting_approval → denied/running`、`running → success/failure/interrupted`，并拒绝 `running → denied`、`denied → running` 和所有终态再转移。
- 审批允许、拒绝与取消的 compare-and-swap 竞态，确保最多只有一个分支生效且工具最多执行一次。
- Git remote、根提交和非 Git 路径三种项目 ID 解析。
- 原子交互块压缩、自适应近期预算、滚动摘要和摘要失败报告。
- 上下文基线不变、增量更新、来源暂时不可用和压缩后替换。
- 全局/项目记忆优先级、原子写入、精确改写和错误返回。
- 模型配置优先级、未配置状态和所有 API Key 脱敏边界。
- 网络重试分类、上下文溢出和最大重试次数。

### 13.2 Orchestrator 集成测试

使用脚本化假 LLM 覆盖：

- 计划 → 读取文件 → 运行失败测试 → 编辑 → 测试通过 → Reflection 完成。
- 非法工具参数被反馈后模型重新调用。
- 用户拒绝写入或命令。
- 连续重复工具调用被终止。
- 达到模型轮次或工具次数上限后生成明确收尾。
- 执行中取消。
- 创建会话并写入用户消息后重启，历史完整恢复。
- assistant 流尚未形成完整工具调用时模拟崩溃：消息保留用于审计，但不进入模型历史，也不执行工具。
- 工具处于 `pending` 或 `awaiting_approval` 时模拟崩溃：生成“未开始、无副作用”的配对 `interrupted` 结果，且不重复执行。
- 工具处于 `running` 时模拟崩溃：生成“结果未知、可能已有副作用”的配对 `interrupted` 结果，且不自动重放。
- 同一 assistant 消息包含多个工具调用，部分已有真实终态结果、部分未闭合：保留真实结果，只为未闭合调用生成结果。
- 连续两次执行恢复收敛，第二次不改变数据、不追加重复 tool result。
- 构造模型历史时遇到孤立结果、重复结果或未收敛调用，返回 `history_integrity_error`，不发起模型请求。
- 压缩后重启，使用摘要、上下文基线和后续消息重建等价请求。
- 普通对话不得自动写记忆；显式“记住/忘掉”写入正确作用域且产生审计事件。
- 项目记忆只进入同一项目，全局记忆进入所有项目，冲突时项目内容优先。
- 摘要失败且请求仍超预算时明确停止，不静默丢弃数据库历史。

### 13.3 Qt 测试

- `RpcCodec` 处理拆包、粘包、中文和转义换行。
- `AgentProcessManager` 与假核心进程完成请求、通知和反向审批。
- 会话列表、新建/加载、历史重放和中断标记正确。
- 未配置模型时禁用运行；保存和测试配置只显示脱敏状态。
- 核心异常退出时 UI 状态正确，重启后可重新加载旧会话。
- 文本缓冲只由定时器批量刷新。

### 13.4 最终人工验收

Qt 中选择演示工作区，输入任务后应能看到：

1. Agent 生成简短计划。
2. 自动读取和搜索源码。
3. 请求批准运行失败测试。
4. 请求批准编辑源码。
5. 再次运行测试并通过。
6. Reflection 检查测试证据。
7. 最终说明修改文件和测试结果。
8. 重启 Core 后重新打开该会话，历史和工具时间线保持完整，且没有工具被重复执行。
9. 明确要求记住一个项目事实，新建会话后该事实仍进入项目上下文。
10. 清除模型配置或使用空白用户数据目录时，界面允许浏览历史但禁止运行并引导主动配置。

## 14. 演示与提交

演示夹具是一个零依赖 Node.js 小项目，包含购物车折扣边界 bug 和失败测试。`demo/reset.mjs` 每次把只读夹具复制到已忽略的 `demo/workspace/`。

固定演示任务：

> 修复购物车折扣边界问题，使全部测试通过，不要修改测试文件；完成后说明改动和测试结果。

视频控制在两分钟内：

- 约 15 秒介绍工作区、模型配置和界面。
- 约 70 秒展示计划、工具调用、审批、修改和测试通过。
- 约 25 秒展示 Orchestrator、工具契约和上下文管理代码。
- 约 10 秒总结设计取舍与限制。

交付要求：

- 根目录 `README.md` 提供完整文档。
- `README.txt` 不超过 1000 汉字，包含仓库地址、运行方法、特色和限制。
- 视频为 mp4，不超过 2 分钟和 200 MB，画面中不得出现凭据。
- 最终 zip 仅包含 `README.txt` 和视频，以本人姓名命名。
- 保留真实、分阶段的 Git 提交历史，截止时间后不再推送。

## 15. OpenCode 参考记录

本地参考仓库版本：

```text
10765ff2a9da8c3b88e4de873aa383a49c318912
```

正式参考以下边界和思想：

| OpenCode 文件 | 借鉴内容 |
| --- | --- |
| `packages/opencode/src/session/prompt.ts` | 显式 Agent 循环、最大步骤和结束条件 |
| `packages/opencode/src/session/processor.ts` | 流事件处理、工具调用状态、重复调用检测 |
| `packages/opencode/src/session/overflow.ts` | 按模型上下文与输出预算计算可用输入量 |
| `packages/opencode/src/session/compaction.ts` | 近期预算为可用量 25%、2K～15K 限制和工具输出截断 |
| `packages/core/src/session/compaction.ts` | 结构化滚动摘要、4096 输出限制和 2000 字符工具摘要 |
| `packages/core/src/session/sql.ts` | 会话、严格顺序消息、持久输入和上下文快照表的职责划分 |
| `packages/core/src/session/history.ts` | 从最新压缩点与上下文基线后重建模型历史 |
| `packages/core/src/session/runner/llm.ts` | 新一次运行开始时将遗留的 pending/running 工具调用收敛为中断错误；不在每次 provider continuation 前结算 |
| `packages/core/src/session/runner/to-llm-message.ts` | 把终态工具状态序列化为与调用 ID 配对的 tool result |
| `packages/core/src/session/context-epoch.ts` | baseline、snapshot、增量更新和压缩后替换 |
| `packages/core/src/system-context/index.ts` | 将动态系统上下文拆为可比较、可更新的来源 |
| `packages/core/src/project.ts` | remote、根提交和路径之间的稳定项目身份策略 |
| `packages/opencode/src/session/instruction.ts` | 全局和项目指令的分层加载 |
| `packages/opencode/src/session/prompt/beast.txt` | 仅在用户明确要求时更新记忆文件；不是通用自动记忆系统 |
| `packages/opencode/src/auth/index.ts` | provider/model 配置与用户数据目录凭据分离 |
| `packages/opencode/src/session/retry.ts` | 可重试错误分类、Retry-After 和指数退避 |
| `packages/opencode/src/session/llm.ts` | 会话逻辑与模型运行时的边界 |
| `packages/opencode/src/session/message-v2.ts` | 将 pending/running 调用兜底转换为中断结果，避免悬空 tool-use 块 |
| `packages/opencode/src/tool/tool.ts` | 统一工具契约和参数错误回馈 |
| `packages/opencode/src/tool/read.ts` | 分页读取、行长和字节上限 |
| `packages/opencode/src/tool/truncate.ts` | 工具输出统一截断 |
| `packages/opencode/src/permission/index.ts` | 权限决策与工具定义分离 |
| `packages/opencode/src/tool/external-directory.ts` | 工作区外路径检测思路 |

有意不照搬的部分：

- 不引入 OpenCode、Effect、Drizzle、AI SDK 或任何 Agent 框架代码；所有重要逻辑自行实现。
- 不实现自动长期记忆；遵循显式“记住/忘掉”的文件式记忆原则。
- 不永久清除 SQLite 中的旧工具输出，只缩减模型可见上下文。
- 不照搬统一字符数除以四的 token 估算，对中文使用更保守估算。
- 不把项目 ID 或运行数据库写入用户代码仓库。
- 不复制 OpenCode 的完整 provider、插件、MCP、工作树或多节点运行体系。

详细阅读过程、源码摘记和个人疑问位于已忽略的 `/.local/opencode-learning.md`，不作为唯一设计依据。

## 16. 设计决策记录

| 编号 | 已确认决策 | 理由 |
| --- | --- | --- |
| D01 | Qt 6 Widgets + TypeScript Core 双进程 | Qt 满足题目桌面要求，TypeScript 便于实现模型和工具循环 |
| D02 | JSON-RPC 2.0 over NDJSON/stdin/stdout | 协议清楚、易测试，不引入网络服务和端口管理 |
| D03 | 单 Orchestrator 执行 Plan → Execute → Reflect | 保留可解释阶段，同时避免多 Agent 协调复杂度 |
| D04 | Node.js 24 LTS + 内置 `node:sqlite` | Node 20 已结束支持；内置 SQLite 提供事务且无第三方原生插件打包负担 |
| D05 | 所有运行数据放在 `%LOCALAPPDATA%\AwaCode\` | 会话、凭据和记忆不污染或泄漏到公开仓库 |
| D06 | remote → 根提交 → 路径的项目 ID 回退 | 支持同一远程仓库不同 clone，又覆盖无 remote 和非 Git 目录 |
| D07 | 完整会话持久化，崩溃后恢复但不自动执行 | 保留上下文，同时避免重复写文件和命令副作用 |
| D08 | 全局 + 项目两级长期记忆，项目优先 | 用户偏好可跨项目，项目事实不会串库 |
| D09 | 仅显式“记住/忘掉”写记忆，不自动提炼 | 忠实参考 OpenCode 的指令文件机制，避免错误记忆污染 |
| D10 | 显式记忆写入不二次确认 | 当前用户指令已授权；通过固定目录和审计事件控制风险 |
| D11 | OpenCode 自适应近期预算，中文保守估算 | 比 75% 和固定六块更有依据，并降低中文 token 低估风险 |
| D12 | 用户必须主动配置 Base URL、模型和 API Key 来源 | 不绑定账号或模型，符合凭据不得入库要求 |
| D13 | 模型配置与 `auth.json` 分离，环境变量优先 | 参考 OpenCode 配置结构并保持仓库无凭据 |
| D14 | 六个工作区工具 + 一个受限记忆工具 | 覆盖 Coding Agent 必需闭环，同时控制截止日期前的实现范围 |
| D15 | 读操作自动允许，工作区写入和命令逐次允许 | 在可用性与本地副作用风险之间保持明确边界 |
| D16 | Qt 定位为 Agent 控制台而非迷你 IDE | 优先展示计划、工具证据、审批、会话和恢复闭环 |
| D17 | OpenCode 原始学习笔记仅保存在已忽略的 `/.local/` | 个人学习过程不公开，正式决策仍集中在本文档 |
| D18 | 工具调用采用显式状态机；恢复先把非终态调用持久化为 `interrupted` 并补齐配对结果 | 防止畸形模型历史和自动重放副作用；完整性修复失败时宁可停止，也不向 API 发送非法请求 |
| D19 | 实施直接在 `master` 完成，每个 green slice 单独提交，仅在里程碑时推送；开发与验证固定使用 Codex 捆绑的 Node 24，桌面端采用 Qt 6.8 LTS/MinGW 13.1 | 保持提交可审计、环境一致，并使用题目期内稳定的 Qt 工具链 |

## 17. 实施顺序与提交建议

1. 固化设计、参考记录和环境要求。提交：`docs: define awacode architecture`
2. 建立 SQLite schema、迁移、项目身份和会话仓储。提交：`feat(core): add durable session storage`
3. 建立核心类型、NDJSON 和双向 JSON-RPC。提交：`feat(core): add ndjson rpc protocol`
4. 实现 WorkspaceGuard、审批接口、六个工作区工具和记忆工具。提交：`feat(core): add workspace tools and approvals`
5. 实现模型配置、LLM 网关、ContextManager 和三阶段 Orchestrator。提交：`feat(core): implement agent orchestration`
6. 实现 Qt 6 Agent 控制台、会话列表和设置页。提交：`feat(desktop): add qt agent console`
7. 加入持久化恢复、记忆、集成、Qt 测试和演示夹具。提交：`test: add agent integration scenarios`
8. 完成 README、视频脚本和提交检查。提交：`docs: finalize submission materials`

每个阶段必须遵循：先编写失败测试或可验证夹具，再实现最小功能，运行相关测试，通过后单独提交。不得为了“提交历史好看”拆分虚假提交，也不得改写已经推送的历史。
